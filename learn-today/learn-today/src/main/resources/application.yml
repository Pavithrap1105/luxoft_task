jwt:
  secret: employee
server:
  port: 8081
spring:
  datasource:
    password: root
    url: jdbc:mysql://localhost:3306/learn_today?useSSL=false
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
  main:
    allow-circular-references: true
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER
      


  
